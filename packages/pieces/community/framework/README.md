# pieces-framework

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pieces-framework` to build the library.

## Running unit tests

Run `nx test pieces-framework` to execute the unit tests via [Jest](https://jestjs.io).

## Features

### [isEndPiece](IS_END_PIECE.md)

Documentation on how to designate an action as a terminal step in a workflow.
